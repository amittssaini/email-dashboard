generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Email {
  id        String   @id
  to        String
  subject   String
  body      String
  sendAt    DateTime
  status    String
  createdAt DateTime @default(now())
  sentAt    DateTime?
}
